import React from 'react'

interface Props {
    data: any, 
    RenderComponent: any,
    pageLimit: any,
    dataLimit: any
}

export  const  Pagination: Reac<Props> =({data, RenderComponent, pagelimit, dataLimit}) =></Props> {
    return (
        <div>
            
        </div>
    )
}
