import React from 'react'

interface Props {
    data: any, 
    RenderComponent: any,
    pageLimit: any,
    dataLimit: any
}

export  const  Pagination: React.FC<Props> =({data, RenderComponent, pagelimit, dataLimit}) =></Props> {
    return (
        <div>
            
        </div>
    )
}
