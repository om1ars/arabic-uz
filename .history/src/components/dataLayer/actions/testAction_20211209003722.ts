import { Dispatch } from "redux";

export const testAction = () => async(dispatch: Dispatch<) =>{
   try {
       dispatch(action: {
           type: "LOADING"
       })
   } catch (
       
   ) {
       
   }

}