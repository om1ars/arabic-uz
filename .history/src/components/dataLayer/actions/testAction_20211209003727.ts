import { Dispatch } from "redux";

export const testAction = () => async(dispatch: Dispatch<DispatchTypes>) =>{
   try {
       dispatch(action: {
           type: "LOADING"
       })
   } catch (
       
   ) {
       
   }

}