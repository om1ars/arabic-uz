import { Dispatch } from "redux";

const testAction = () => async(dispatch: Dispatch) ={
    try {
        dispatch(action: {
            type: "TEST_LOA"
        })
        
    } catch (error) {
        
    }

}