import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const testSlice = createSlice({
    name: sliceName,
    initialState,
    reducers: {
    
    }
});

export const {

} = testSlice.actions
export default testSlice.reducer