import { createSlice } from '@reduxjs/toolkit'

const initialState = {
value: 0
}

const testSlice = createSlice({
    name: sliceName,
    initialState,
    reducers: {
    
    }
});

export const {

} = testSlice.actions
export default testSlice.reducer