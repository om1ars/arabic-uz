import React from 'react'

interface Props {
    data: any, 
    RenderComponent: any,
    pageLimit: 
}

export default function Pagination({data, RenderComponent, pagelimit, dataLimit}) {
    return (
        <div>
            
        </div>
    )
}
