import React from 'react'

interface Props {
    data: any, 
    RenderComponent: any,
    pageLimit: any,
    dataLimit: any
}

export default function Pagination<P({data, RenderComponent, pagelimit, dataLimit}) {
    return (
        <div>
            
        </div>
    )
}
